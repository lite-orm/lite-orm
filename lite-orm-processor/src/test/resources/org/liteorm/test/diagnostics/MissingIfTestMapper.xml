<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="org.liteorm.test.diagnostics.MissingIfTestMapper">
    <select id="findValue" resultType="org.liteorm.test.diagnostics.ValueRow">
        SELECT 'value'
        <if>WHERE value = #{value}</if>
    </select>
</mapper>
