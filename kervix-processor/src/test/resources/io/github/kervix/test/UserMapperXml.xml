<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="io.github.kervix.test.UserMapperXml">
    
    <!-- Simple query -->
    <select id="findById" resultType="io.github.kervix.test.User">
        SELECT * FROM users WHERE id = #{id}
    </select>
    
    <!-- Dynamic query -->
    <select id="findByCondition" resultType="io.github.kervix.test.User">
        SELECT * FROM users
        <where>
            <if test="name != null">
                AND name = #{name}
            </if>
            <if test="age != null">
                AND age = #{age}
            </if>
            <choose>
                <when test="status != null">
                    AND status = #{status}
                </when>
                <otherwise>
                    AND status = 'ACTIVE'
                </otherwise>
            </choose>
        </where>
    </select>
    
    <!-- Dynamic insert -->
    <insert id="insertUser">
        INSERT INTO users
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null">name,</if>
            <if test="age != null">age,</if>
            <if test="email != null">email,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null">#{name},</if>
            <if test="age != null">#{age},</if>
            <if test="email != null">#{email},</if>
        </trim>
    </insert>
    
    <!-- Dynamic update -->
    <update id="updateUser">
        UPDATE users
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="age != null">age = #{age},</if>
            <if test="email != null">email = #{email},</if>
        </set>
        WHERE id = #{id}
    </update>
    
    <!-- Batch operation -->
    <insert id="batchInsert">
        INSERT INTO users (name, age, email) VALUES
        <foreach collection="list" item="user" separator=",">
            (#{user.name}, #{user.age}, #{user.email})
        </foreach>
    </insert>
    
    <!-- Complex query -->
    <select id="findUsersWithPagination" resultType="io.github.kervix.test.User">
        SELECT * FROM users
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') OR email LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="minAge != null">
                AND age >= #{minAge}
            </if>
            <if test="maxAge != null">
                AND age &lt;= #{maxAge}
            </if>
            <if test="statusList != null and statusList.size() > 0">
                AND status IN
                <foreach collection="statusList" item="status" open="(" close=")" separator=",">
                    #{status}
                </foreach>
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>
    
</mapper>
