<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="org.liteorm.test.diagnostics.MalformedXmlMapper">
    <select id="findValue" resultType="org.liteorm.test.diagnostics.ValueRow">
        SELECT value
        <if test="value != null">
            FROM values_table
    </select>
</mapper>
