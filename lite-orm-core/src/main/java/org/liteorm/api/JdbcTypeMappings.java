package org.liteorm.api;

/**
 * Marks a compile-time collection of JDBC value mappings for one database family.
 *
 * <p>The collection describes value conversion only. It does not define SQL dialect,
 * schema, transactions, routing, pagination, or generated-key behavior.</p>
 */
public interface JdbcTypeMappings {
}
